<h1>User\Company creation</h1>

<form action="{{ path('staff_company_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('staff_company') }}">
            Back to the list
        </a>
    </li>
</ul>
